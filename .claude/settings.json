{
  "// What this is": "Per-project Claude Code config. Hooks run automatically every time Claude Code uses a tool — they're how the harness enforces workflow rules instead of relying on Claude remembering them.",
  "// Brain reference": "/Users/lost/Documents/Brain/00 Projects/Hikebeast/04-site/dev-workflow.md",

  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "${CLAUDE_PROJECT_DIR}/.claude/workflow-guard.sh"
          }
        ]
      }
    ]
  }
}
