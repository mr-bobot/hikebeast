# Hikebeast — Swiss-made guides, filters and grades for alpine creators
# https://hikebeast.ch
#
# Note: do NOT add Disallow rules for paths that need to be scraped by
# social-media link-preview bots (facebookexternalhit, Twitterbot, etc).
# Vercel's edge WAF treats Disallow as actual access control for known
# crawler UAs, so any Disallow line on a path the scraper hits returns
# 403 and breaks the rich link preview. SEO-side, the noindex <meta>
# tag on each page is what keeps it out of search results · robots.txt
# is purely for crawl-budget hints. Pattern set by commit 720c103
# (2026-04-27) when /free/download/ had the same 403 issue.

User-agent: *
Allow: /
# Note: no Disallow rules in the wildcard block. Vercel's edge WAF
# treats Disallow as access control and gets paranoid about crawler
# UAs (incl. facebookexternalhit) when the wildcard block has any
# Disallow lines, returning 403 even on paths that aren't disallowed.
# /api/, /preview/, /full/ are blocked from the AI-bot blocks below
# (those crawlers respect robots.txt politely). Path-level access
# control for /full/ is enforced by middleware.js + vercel.json
# X-Robots-Tag headers + per-page <meta robots>; robots.txt is just
# crawl-budget signal. See commit history May 4-10 (FB 403 issue).

# AI search crawlers — explicitly disallowed from /full/ and /preview/
# (those are paid-customer / pre-launch areas, not for ingestion)
User-agent: GPTBot
Allow: /
Disallow: /api/
Disallow: /preview/
Disallow: /full/

User-agent: ClaudeBot
Allow: /
Disallow: /api/
Disallow: /preview/
Disallow: /full/

User-agent: Claude-Web
Allow: /
Disallow: /api/
Disallow: /preview/
Disallow: /full/

User-agent: PerplexityBot
Allow: /
Disallow: /api/
Disallow: /preview/
Disallow: /full/

User-agent: Google-Extended
Allow: /
Disallow: /api/
Disallow: /preview/
Disallow: /full/

User-agent: CCBot
Allow: /
Disallow: /api/
Disallow: /preview/
Disallow: /full/

User-agent: Applebot-Extended
Allow: /
Disallow: /api/
Disallow: /preview/
Disallow: /full/

User-agent: Bingbot
Allow: /
Disallow: /api/
Disallow: /preview/
Disallow: /full/

# Social-media link-preview crawlers · explicit allow blocks with NO
# Disallow rules. Facebook's debugger literal-parses for a per-UA block
# and falls back to a misleading "robots.txt block · please allowlist"
# error when one is missing. Doing exactly what their error message
# says to do.
User-agent: facebookexternalhit
Allow: /

User-agent: facebookcatalog
Allow: /

User-agent: Twitterbot
Allow: /

User-agent: LinkedInBot
Allow: /

User-agent: Slackbot
Allow: /

User-agent: WhatsApp
Allow: /

User-agent: TelegramBot
Allow: /

Sitemap: https://hikebeast.ch/sitemap.xml
